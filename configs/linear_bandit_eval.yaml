# Evaluation configuration for linear bandit environment
env: "linear_bandit"
dim: 10
lin_d: 2
var: 0.3
cov: 0.0
test_cov: -1.0

# Model architecture (needed to create model before loading weights)
embd: 32
head: 4
layer: 4
dropout: 0

# Evaluation configuration
n_eval: 200
hor: 500
seed: 1